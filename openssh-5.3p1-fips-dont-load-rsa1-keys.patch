diff --git a/authfile.c b/authfile.c
index d275296..7cf1c6d 100644
--- a/authfile.c
+++ b/authfile.c
@@ -609,9 +609,12 @@ key_load_private_type(int type, const char *filename, const char *passphrase,
 		*perm_ok = 1;
 	switch (type) {
 	case KEY_RSA1:
-		return key_load_private_rsa1(fd, filename, passphrase,
-		    commentp);
-		/* closes fd */
+		if ( ! FIPS_mode())
+			return key_load_private_rsa1(fd, filename, passphrase,
+			    commentp);
+			/* closes fd */
+		close(fd);
+		return NULL;
 	case KEY_DSA:
 	case KEY_ECDSA:
 	case KEY_RSA:
@@ -652,7 +652,10 @@ key_load_private(const char *filename, const char *passphrase,
 		/* it's a SSH v1 key if the public key part is readable */
 		key_free(pub);
 		/* closes fd */
-		prv = key_load_private_rsa1(fd, filename, passphrase, NULL);
+		if ( ! FIPS_mode())
+			prv = key_load_private_rsa1(fd, filename, passphrase, NULL);
+		else
+			close(fd);
 	}
 	return prv;
 }
