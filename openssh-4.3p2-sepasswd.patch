diff -up openssh-4.3p2/session.c.sepasswd openssh-4.3p2/session.c
--- openssh-4.3p2/session.c.sepasswd	2011-08-24 18:50:09.299023348 +0200
+++ openssh-4.3p2/session.c	2011-08-24 18:54:14.811029994 +0200
@@ -1409,6 +1409,9 @@ do_pwchange(Session *s)
 	if (s->ttyfd != -1) {
 		fprintf(stderr,
 		    "You must change your password now and login again!\n");
+#ifdef WITH_SELINUX
+		setexeccon(NULL);
+#endif
 #ifdef PASSWD_NEEDS_USERNAME
 		execl(_PATH_PASSWD_PROG, "passwd", s->pw->pw_name,
 		    (char *)NULL);
